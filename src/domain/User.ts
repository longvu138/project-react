export interface IUser {
    username: string
    email: string
    fullname: string
    avatar: string
    gender: string
    birthday: Date
    createdAt: Date
    updatedAt: Date
    config: JSON
}